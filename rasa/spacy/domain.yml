version: "2.0"

intents:
  - opt_log
  - opt_test

entities:
  - print
  - log_level
  - message

slots:
  print:
    type: text
    influence_conversation: false
  log_level:
    type: text
    influence_conversation: false
  message:
    type: text
    influence_conversation: false

responses:
  utter_greate:
  - text: "fine"

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
